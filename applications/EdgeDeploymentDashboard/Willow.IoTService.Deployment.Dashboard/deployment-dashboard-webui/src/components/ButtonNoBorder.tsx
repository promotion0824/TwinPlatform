import { Button, IconButton } from "@willowinc/ui";
import styled from "styled-components";

export const IconButtonNoBorder = styled(IconButton)({
  border: "none",
  outline: "none!important",
});

export const ButtonNoBorder = styled(Button)({
  border: "none",
  outline: "none!important",
});
