import { ReactNode, ReactElement } from 'react'

/**
 * @deprecated Please use `MoreButtonDropdown` instead.
 */
export default function MoreDropdownButton(props: {
  children?: ReactNode
  icon?: string
  onClick?: () => void
}): ReactElement
