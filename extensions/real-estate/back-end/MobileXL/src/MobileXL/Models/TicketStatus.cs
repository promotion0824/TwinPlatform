namespace MobileXL.Models
{
    public enum TicketStatus
    {
        Open = 0,
        Reassign = 5,
        InProgress = 10,
        LimitedAvailability = 15,
        Resolved = 20,
        Closed = 30
    }
}
