namespace InsightCore.Models
{
    public enum InsightStatus
    {
        Open = 0,
        Ignored = 10,
        InProgress = 20,
        Resolved = 30,
		New = 40,
		Deleted = 50,
        ReadyToResolve = 60
    }
}
