import styled from 'styled-components'
import { Icon } from '../../../misc/Icon'

const DataGridIcon = styled(Icon)`
  color: ${({ theme }) => theme.color.neutral.fg.default};
`
export default DataGridIcon
