import { Meta } from '@storybook/blocks'
import { ArgTypes, ToMantineProps } from '../../../docs/_components'

import { DateTimeInput } from '.'
import * as stories from './DateTimeInput.stories'

<Meta of={stories} />

# Props

## DateTimeInput

It can accept all props available in `TextInput` component as root props, for
example `labelProps` and `descriptionProps`.

<ArgTypes of={DateTimeInput} />

<ToMantineProps componentName="DateTimeInput" />
